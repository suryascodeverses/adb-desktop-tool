{
  "name": "@adb/electron",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "vite build --config vite.main.config.ts && vite build --config vite.preload.config.ts && cd . && electron dist/main.js",
    "build": "vite build --config vite.main.config.ts && vite build --config vite.preload.config.ts",
    "package:win": "electron-builder --win",
    "package:mac": "electron-builder --mac",
    "package:linux": "electron-builder --linux"
  },
  "devDependencies": {
    "electron-builder": "^24.9.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "electron": "^33.4.11"
  }
}
